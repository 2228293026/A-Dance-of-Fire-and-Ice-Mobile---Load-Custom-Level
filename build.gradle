// 根 build.gradle
buildscript {
    repositories {
        google()
        jcenter()
    }
    dependencies {
        // 使用广泛兼容的 AGP 版本（AIDE 也支持）
        classpath 'com.android.tools.build:gradle:4.1.3'
    }
}

allprojects {
    repositories {
        google()
        jcenter()
    }
}
